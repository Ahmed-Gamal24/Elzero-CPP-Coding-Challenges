
  int help_num = 4;
  int nums[] = {2, 4, 5, 6, 10};
  for (int i = 0; i < size(nums); i++)
  {
      cout << nums[i] << " + " << nums[(size(nums)) - (i+1)] ;
      cout << " = " << nums[i] + nums[(size(nums)) - (i+1)]  << "\n";
  }
  //without using varuable (help_num) : )

                            